import PageLayout from '../components/layout/PageLayout'

export default function ShiftPage() {
  return (
    <PageLayout title="Mijn Dienst">
      <p style={{ color: 'var(--color-text-muted)' }}>Shift scherm — komt nog.</p>
    </PageLayout>
  )
}